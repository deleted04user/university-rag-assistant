services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: univ
      POSTGRES_PASSWORD: univ
      POSTGRES_DB: univ
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
